# Orbit Command Center (OpenUI Demo)

A focused, static frontend mock of a premium dark cockpit dashboard for Orbit.

## Run locally

```bash
cd frontend/openui-orbit-demo
npm install
npm run dev
```

The page is fully static and uses mock data from `src/data/orbitDemoData.js`.
